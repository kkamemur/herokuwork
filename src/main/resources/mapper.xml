<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MaaperInterfaceImplemented.FQCN">
	<select id="count" resultType="ResultType.FQCN">
		SELECT
		  FROM
		  WHERE
		  ORDER BY
    </select>
</mapper>